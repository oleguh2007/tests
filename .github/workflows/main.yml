name: Print Repository Name
on: [push, workflow_dispatch]

jobs:
  print-repo-name:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node_ip: [1, 0]
    steps:
      - name: Print HOST IP
        run: echo "${{ fromJson(matrix.node_ip) }}"
